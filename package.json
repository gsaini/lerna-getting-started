{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "publish": "lerna publish from-package",
    "start": "lerna run lerna-test"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}
